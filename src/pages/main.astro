---
import Layout from '../layouts/Default.astro'
import NewForm from '../components/vue/MainForm.vue'
import NavBar from '../components/vue/Navbar.vue'
import '../assets/sytles/style.astro'

const { email, id } = Astro.locals
export {email, id}
---

<Layout title="Test Vue">
	<NavBar client:visible />
	<NewForm client:visible email={email} id={id} />
</Layout>
